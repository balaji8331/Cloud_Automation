{
  "name": "azure-terminal-server",
  "version": "1.0.0",
  "description": "WebSocket sidecar that manages Docker-isolated Azure CLI terminal sessions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "cookie": "^0.6.0",
    "dockerode": "^4.0.2",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}
